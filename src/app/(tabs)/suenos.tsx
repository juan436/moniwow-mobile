/**
 * SuenosTab — Layout (app route)
 *
 * @what Wrapper expo-router del tab Sueños. Renderiza SuenosScreen.
 */
import { SuenosScreen } from '@features/suenos/components/SuenosScreen';

export default function SuenosTab() {
  return <SuenosScreen />;
}
