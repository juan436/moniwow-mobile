/**
 * DashboardTab — Layout (app route)
 *
 * @what Wrapper expo-router del tab Inicio. Renderiza DashboardScreen.
 */
import { DashboardScreen } from '@features/dashboard/components/shared/DashboardScreen';

export default function DashboardTab() {
  return <DashboardScreen />;
}
