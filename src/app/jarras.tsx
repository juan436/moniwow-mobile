/**
 * JarrasTab — Screen (app route)
 *
 * @what     Wrapper expo-router de la ruta "Mis jarras". Renderiza JarsScreen.
 * @returns  JSX — JarsScreen.
 */
import { JarsScreen } from '@features/jars/components/JarsScreen';

export default function JarrasTab() {
  return <JarsScreen />;
}
